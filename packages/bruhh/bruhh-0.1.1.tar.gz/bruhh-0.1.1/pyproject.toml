[tool.poetry]
name = "bruhh"
version = "0.1.1"
description = ""
authors = ["abi_jey <abi-jey@users.noreply.github.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pydantic = ">=2.10.0"
typer = ">=0.14.0"
rich = "*"

[tool.poetry.group.daemon.dependencies]
fastapi = {extras = ["all"], version = "^0.115.6"}
uvicorn = {extras = ["all"], version = "^0.34.0"}
pydantic-ai = {extras = ["logfire", "graph", "openai", "vertexai", "anthropic", "groq", "mistral"], version = "^0.0.19"}

[tool.poetry.scripts]
bruhh = "bruhh.cli.main:app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
