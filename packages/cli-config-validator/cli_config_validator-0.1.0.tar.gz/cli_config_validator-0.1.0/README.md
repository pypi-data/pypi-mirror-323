# cli_config_validator

> Validate configuration from multiple configuration files against a Python pydantic class Model.

## Usage

```sh
./cli_config_validator.py -v config.yaml -v config2.yaml -m model.py -c ConfigModel
```
