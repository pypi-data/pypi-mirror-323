[project]
name = "huey-mongodb"
version = "0.1.0"
description = "huey MongoDB backend"
authors = [
    { name = "Leon", email = "leon0824@gmail.com" },
]
dependencies = [
    "huey>=2.5.2",
    "pymongo>=4.10.1",
]
requires-python = ">=3.9"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
