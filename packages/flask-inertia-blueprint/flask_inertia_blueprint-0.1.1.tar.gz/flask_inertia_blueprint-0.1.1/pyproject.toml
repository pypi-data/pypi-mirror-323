[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flask-inertia-blueprint"
version = "0.1.1"
requires-python = ">=3.7"
dependencies = [
    "flask>=1.1.2",
    "jsmin>=2.2.2"
]