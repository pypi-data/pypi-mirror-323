[tool.poetry]
name = "ume-shippy"
version = "0.0.11"
description = "A library with helper functions for the Smart Hospital Information Platform (SHIP)"
authors = ["Felix Nensa <felix.nensa@uk-essen.de>"]
packages = [{ include = "shippy" }]
license = "MIT"
readme = "README.md"
homepage = "https://github.com/UMEssen/shippy"

[tool.poetry.dependencies]
python = ">=3.12"
aiohttp = "^3.11.11"
chardet = "^5.2.0"
charset-normalizer = "^3.4.1"
requests = "^2.32.3"
huggingface-hub = "^0.27.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
