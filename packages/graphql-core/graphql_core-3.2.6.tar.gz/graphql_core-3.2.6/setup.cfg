[bdist_wheel]
python-tag = py3

[aliases]
test = pytest

[tool:pytest]
addopts = --benchmark-disable
python_classes = PyTest*
asyncio_mode = auto
timeout = 100
filterwarnings = ignore::pytest.PytestConfigWarning

[egg_info]
tag_build = 
tag_date = 0

