[build-system]
requires = ["setuptools", "wheel"]  # Dependencies for building the package
build-backend = "setuptools.build_meta"

[project]
name = "py-vikunja"
version = "0.1"
description = "A Python wrapper for Vikunja API"
authors = [
    {name = "Joseph Shufflebotham"}
]
license = {text = "AGPL"}
dependencies = [
    "httpx==0.28.1"
]