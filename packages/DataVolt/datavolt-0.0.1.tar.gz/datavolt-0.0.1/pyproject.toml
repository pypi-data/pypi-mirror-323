[tool.poetry]
name = "DataVolt"
version = "0.0.1"
description = "A reusable workflow for data engineering pipelines"
authors = ["Allan <allanw.mk@gmail.com>"]

[tool.poetry.dependencies]
python = "3.10"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
