[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "zkwasm-minirollup-rpc-py"
version = "0.1.0"
description = "TODO"
readme = "README.md"
authors = [{ name = "Arriviste", email = "1900017707@pku.edu.cn" }]

[tool.poetry.dependencies]
python = ">=3.9,<4.0"  # 确保仅支持 3.9 及以上版本
requests = ">=2.25.1,<2.32.0"  # 限制 requests 版本范围