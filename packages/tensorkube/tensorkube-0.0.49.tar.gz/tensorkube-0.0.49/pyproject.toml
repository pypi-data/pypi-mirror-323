[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tensorkube"

version = "0.0.49"

authors = [{ name = "Samagra", email = "samagra14@tensorfuse.io" },
    { name = "Divyanshu", email = "divyanshu@tensorfuse.io" },
    { name = "Ganesh", email = "ganesh@tensorfuse.io"}
]
description = "Python SDK for Tensorfuse"
readme = "README.md"
requires-python = ">=3.7"
classifiers = ["Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    'boto3',
    'kubernetes==31.0.0',
    'click==8.1.7',
    'tqdm', 
    'requests',
    'setuptools',
    'pathspec',
    'rich',
    'inquirer',
    'metapod==0.1.16',
    'semver==3.0.2'
]

[project.scripts]
tensorkube = "tensorkube.services.cli:tensorkube"

