[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python_sch_api"
version = "0.0.1"
dependencies = [
  "requests",
]
requires-python = ">=3.8"
authors = [
  { name="Semyon Terebenin", email="semenchik02@gmail.com" },
]
description = "Python 3 пакет для взаимодействия с sch-api"
readme = "README.md"

classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = { file = "LICENSE" }

[project.urls]
Homepage = "http://gitea.shgpi/aneri/pypi-sch-api"
Issues = "http://gitea.shgpi/aneri/pypi-sch-api/issues"