name: example
description: A python program using the component provider
runtime:
  name: python
  options:
    toolchain: uv
plugins:
  providers:
    - name: my-component
      path: ../my-component
