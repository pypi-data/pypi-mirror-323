{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Pypi: publish",
            "type": "shell",
            "command": "/bin/bash",
            "args": [
                "-c",
                "rm -rf dist && VERSION=$(cat VERSION) && TWINE_USERNAME=__token__ python setup.py sdist && twine upload dist/*"
            ],
            "problemMatcher": []
        }
    ]
}