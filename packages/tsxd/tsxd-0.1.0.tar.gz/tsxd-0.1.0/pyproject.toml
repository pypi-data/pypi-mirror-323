[project]
name = "tsxd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jviduz", email = "Jviduz@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
tsxd = "tsxd:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
