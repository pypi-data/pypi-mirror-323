[project]
name = "embedding-suite"
version = "0.1.3"
description = "A light wrapper around various embedding providers"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "cohere>=5.13.11",
    "openai>=1.60.1",
    "python-dotenv>=1.0.1",
]
