# >>> import pathlib
# >>> import configparser
#
# >>> dataclock_ini = pathlib.Path("config/dataclock.ini")
# >>> config = configparser.ConfigParser()
# >>> config.read(dataclock_ini)
# >>> config.get("DEFAULT", "TITLE")
# 'Data Clock Chart'
# >>> config.get("mode.description", "DOW_HOUR")
# 'day of week (rings) & hour of day (wedges)'

[DEFAULT]
TITLE = Data Clock Chart

[mode.description]
YEAR_MONTH = year (rings) & month (wedges)
YEAR_WEEK = year (rings) & week of year (wedges)
WEEK_DAY = week of year (rings) & day of week (wedges)
DOW_HOUR = day of week (rings) & hour of day (wedges)
DAY_HOUR =  day of year (rings) & hour of day (wedges)
