"""
This setup.py is kept for backwards compatibility with pip install -e.
The actual build configuration is in pyproject.toml and CMakeLists.txt.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
